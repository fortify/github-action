name: 'Package source code & dependencies'
description: 'Package source code & dependencies for SAST & SCA scans'
author: 'Fortify'
runs:
  using: composite
  steps:
    - uses: fortify/github-action/internal/setup-java-for-sc-client@feat-version-override
    - uses: fortify/github-action/setup@feat-version-override
      with:
        export-path: false
        sc-client: action-default
    - uses: fortify/github-action/internal/run-script@feat-version-override
      with:
        script: package.sh
        
branding:
  icon: 'shield'
  color: 'blue'

