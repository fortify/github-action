name: 'Package source code & dependencies'
description: 'Package source code & dependencies for SAST & SCA scans'
author: 'Fortify'
runs:
  using: composite
  steps:
    - uses: fortify/github-action/internal/setup-java-for-sc-client@feat-1.3.0
    - uses: fortify/github-action/setup@feat-1.3.0
      with:
        export-path: false
        sc-client: action-default
    - uses: fortify/github-action/internal/run-script@feat-1.3.0
      with:
        script: package.sh
        
branding:
  icon: 'shield'
  color: 'blue'

