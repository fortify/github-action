name: 'Package source code & dependencies'
description: 'Package source code & dependencies for SAST & SCA scans'
author: 'Fortify'
runs:
  using: composite
  steps:
    - uses: fortify/github-action/internal/setup-java-for-sc-client@release-please--branches--main--components--github-action
    - uses: fortify/github-action/setup@release-please--branches--main--components--github-action
      with:
        export-path: false
        sc-client: action-default
    - uses: fortify/github-action/internal/run-script@release-please--branches--main--components--github-action
      with:
        script: package.sh
        
branding:
  icon: 'shield'
  color: 'blue'

