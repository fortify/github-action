name: 'Perform SAST scan'
description: 'Perform a SAST scan on Fortify on Demand'
author: 'Fortify'
runs:
  using: composite
  steps:
    - uses: fortify/github-action/internal/set-fod-var-defaults@v1.6.4
    - uses: fortify/github-action/setup@v1.6.4
      with:
        export-path: false
        fcli: action-default
    - uses: fortify/github-action/internal/fod-login@v1.6.4
    - uses: fortify/github-action/package@v1.6.4
    - uses: fortify/github-action/internal/run-script@v1.6.4
      with:
        script: fod-scan.sh
      env: 
        DO_SAST_SCAN: true # Not used for now
        GITHUB_TOKEN: ${{ github.token }}
    - if: env.DO_EXPORT == 'true'
      uses: fortify/github-action/fod-export@v1.6.4
        
branding:
  icon: 'shield'
  color: 'blue'

