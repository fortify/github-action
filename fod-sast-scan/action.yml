name: 'Perform SAST scan'
description: 'Perform a SAST scan on Fortify on Demand'
author: 'Fortify'
runs:
  using: composite
  steps:
    - uses: fortify/github-action/internal/set-fod-var-defaults@feat-1.3.0
    - uses: fortify/github-action/setup@feat-1.3.0
      with:
        export-path: false
        fcli: action-default
    - uses: fortify/github-action/internal/fod-login@feat-1.3.0
    - uses: fortify/github-action/package@feat-1.3.0
    - uses: fortify/github-action/internal/run-script@feat-1.3.0
      with:
        script: fod-scan.sh
      env: 
        DO_SAST_SCAN: true # Not used for now
        GITHUB_TOKEN: ${{ github.token }}
    - if: env.DO_EXPORT == 'true'
      uses: fortify/github-action/fod-export@feat-1.3.0
        
branding:
  icon: 'shield'
  color: 'blue'

