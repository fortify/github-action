name: 'Perform SAST scan'
description: 'Perform a SAST scan on Fortify on Demand'
author: 'Fortify'
runs:
  using: composite
  steps:
    - uses: fortify/github-action/internal/set-fod-var-defaults@dev/v2.x
    - uses: fortify/github-action/setup@dev/v2.x
      with:
        export-path: false
        fcli: action-default
    - uses: fortify/github-action/internal/fod-login@dev/v2.x
    - uses: fortify/github-action/package@dev/v2.x
    - uses: fortify/github-action/internal/run-script@dev/v2.x
      with:
        script: fod-scan.sh
      env: 
        DO_SAST_SCAN: true # Not used for now
        GITHUB_TOKEN: ${{ github.token }}
    - if: env.DO_EXPORT == 'true'
      uses: fortify/github-action/fod-export@dev/v2.x
        
branding:
  icon: 'shield'
  color: 'blue'

