name: 'Fortify AST Scan'
description: 'Build secure software fast with Fortify code security.'
author: 'Fortify'
inputs:
  sast-scan:
    description: 'Run a SAST scan, takes either true or false (default)'
    default: 'false'
    required: false
runs:
  using: composite
  steps:
    - uses: fortify-ps/github-action/fod-sast-scan@main
      if: inputs['sast-scan']=='true' && env.FOD_URL
    - uses: fortify-ps/github-action/sc-sast-scan@main
      if: inputs['sast-scan']=='true' && env.SSC_URL  
        
branding:
  icon: 'shield'
  color: 'blue'

